---
interface Props {
	id?: string;
	variant?: 'action' | 'primary' | 'download' | 'reset' | 'small';
	class?: string;
}

const { id, variant = 'action', class: className } = Astro.props;
---

<button id={id} class:list={['btn', `btn-${variant}`, className]}>
	<slot />
</button>

<style>
	.btn {
		padding: 10px 15px;
		border: none;
		border-radius: 6px;
		cursor: pointer;
		font-weight: 500;
		transition: all 0.2s;
		display: inline-flex;
		align-items: center;
		gap: 8px;
	}

	.btn-action {
		background-color: #f0f0f0;
		color: #333;
	}
	.btn-action:hover {
		background-color: #e0e0e0;
	}

	.btn-primary {
		background-color: var(--primary-color);
		color: white;
		padding: 12px 18px;
		font-size: 16px;
	}
	.btn-primary:hover {
		background-color: var(--primary-color-hover);
		transform: translateY(-1px);
	}

	.btn-download {
		background-color: var(--secondary-color);
		color: white;
		padding: 12px 18px;
		font-size: 16px;
	}
	.btn-download:hover {
		background-color: var(--secondary-color-hover);
		transform: translateY(-1px);
	}

	.btn-reset {
		background-color: #f5f5f5;
		color: #666;
	}
	.btn-reset:hover {
		background-color: #e0e0e0;
		color: #444;
	}

	.btn-small {
		background-color: var(--danger-color);
		color: white;
		padding: 6px 10px;
		font-size: 0.8rem;
	}
	.btn-small:hover {
		background-color: var(--danger-color-hover);
	}
</style>
